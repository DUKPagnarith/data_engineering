# ── Weather Project (ignore) ──
dags/etl_weather_dag.py
streamlit_app/app.py

# ── Apple Stock Project (ignore) ──
etl_apple_stock.ipynb
etl_pipeline_apple_stock.ipynb
dags/elt_apple_stock_dag.py

# ── MongoDB (ignore) ──
docker-compose-mongo.yml

# ── Airflow logs ──
logs/

# ── Data files (keep CSV out of git) ──
data/

# ── Python cache ──
__pycache__/
*.pyc

# ── macOS ──
.DS_Store

# ── Other non-employee files ──
requirements.txt
README.md
